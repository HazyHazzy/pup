<div class="breed-cards">
  <% @breeds.reverse_each do |breed|%>
    <div class="breed-category breed-index-img" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url( <%= cl_image_path breed.photo.key %>)">
      <%= link_to breed.name, breed_path(breed.id), class: "breed-name" %>
    </div>
  <% end %>
</div>

